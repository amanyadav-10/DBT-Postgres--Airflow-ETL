# Copy this file to ~/.dbt/profiles.yml (or set DBT_PROFILES_DIR to this folder)
# and adjust credentials if you changed the defaults in docker-compose.yml.

cricket_analytics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: airflow
      password: airflow
      port: 5432
      dbname: cricket
      schema: public
      threads: 4
